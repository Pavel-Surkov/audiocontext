export const DEFAULT_POINTS_COUNT = 10;
export const DEFAULT_RADIUS = 200;

export const STAGE_WIDTH = window.innerWidth;
export const STAGE_HEIGHT = window.innerHeight;

export const HALF_WIDTH = STAGE_WIDTH / 2;
export const HALF_HEIGHT = STAGE_HEIGHT / 2;

// In ms
export const SIN_ANIMATION_PERIOD = 2000;
export const SIN_ANIMATION_AMPLITUDE = 100;
